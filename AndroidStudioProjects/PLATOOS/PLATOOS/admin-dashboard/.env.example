# Delito Admin Dashboard - Environment Variables
# Copy this file to .env.local and fill in your values

# ===========================================
# FIREBASE ADMIN SDK CONFIGURATION
# ===========================================
# Get these from Firebase Console > Project Settings > Service Accounts
# Download the service account JSON and extract the values

FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# ===========================================
# RAZORPAY CONFIGURATION
# ===========================================
# Get these from Razorpay Dashboard > Settings > API Keys
# https://dashboard.razorpay.com/app/keys

# Razorpay API Keys (Required for payments & refunds)
# Test keys start with: rzp_test_
# Live keys start with: rzp_live_
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxx
RAZORPAY_KEY_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxx

# ===========================================
# RAZORPAY X (PAYOUTS) - OPTIONAL
# ===========================================
# NOTE: RazorpayX is ONLY needed for automated bank payouts
# In TEST MODE, you do NOT need this - payouts will be recorded manually
#
# For LIVE mode with automated payouts:
# 1. Apply for RazorpayX at https://razorpay.com/x/
# 2. Once approved, you'll get a business account number
#
# Leave empty or remove for test mode / manual payout tracking
RAZORPAY_ACCOUNT_NUMBER=

# ===========================================
# OPTIONAL: ADDITIONAL CONFIGURATION
# ===========================================

# Base URL for the admin dashboard (used for webhooks, etc.)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Enable debug mode (set to 'true' for development)
DEBUG_MODE=true

# ===========================================
# NOTES
# ===========================================
#
# 1. FIREBASE PRIVATE KEY:
#    - Make sure to keep the \n characters in the key
#    - Wrap the entire key in double quotes
#    - The key should start with -----BEGIN PRIVATE KEY-----
#
# 2. RAZORPAY TEST vs LIVE:
#    - Test keys start with: rzp_test_
#    - Live keys start with: rzp_live_
#    - Use test keys during development
#    - Refunds work in test mode with test keys
#
# 3. PAYOUTS (Vendor/Delivery Partner):
#    - In TEST MODE: Payouts are tracked in Firebase only (no actual bank transfer)
#    - In LIVE MODE with RazorpayX: Automated bank transfers
#    - You can mark payouts as "completed" manually in the admin panel
#
# 4. COD SETTLEMENTS:
#    - Always tracked in Firebase
#    - No Razorpay integration needed for COD
#    - Admin marks settlement when delivery partner returns cash
#
# 5. SECURITY:
#    - Never commit .env.local to version control
#    - Add .env.local to your .gitignore file
#    - Rotate keys periodically

